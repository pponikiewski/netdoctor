# netdoc — Session Notes
<!-- One line per commit, appended via /sync: date | commit subject | what/why -->

- 2026-09-20 | feat: enhance outage analysis and logging with new metrics and improved handling | Rozbudowa analizy awarii i zapisu zdarzeń w cause/diagnose/store, żeby historia niosła więcej niż sam fakt przerwy.
- 2026-09-20 | feat(live): make the chart and the cards readable without prior knowledge | Zakładka Na żywo tłumaczy teraz każdą liczbę: klikalne chipy legendy z odczytem, wyjaśnienia celów i kafelków pod kursorem, podpisane progi, uporządkowana legenda pod wykresem; przy okazji usunięte zapytania DNS i SQLite wykonywane przy każdej klatce.
- 2026-09-20 | style: format the codebase with rustfmt, pinned by rustfmt.toml | Kod był formatowany ręcznie i nie pasował do żadnej konfiguracji rustfmt, więc `cargo fmt` wymagany przez CLAUDE.md rozsypywał diff po kilkunastu modułach; jednorazowe przeformatowanie z przypiętym `use_small_heuristics = "Max"`.
- 2026-09-20 | chore: keep the formatting commit out of git blame | `.git-blame-ignore-revs` z hashem commita formatującego, żeby blame dalej wskazywał autora treści, a nie przebiegu formatowania.
- 2026-09-20 | docs: sync notes, architecture and project instructions | Zaległe zmiany w dokumentach i instrukcjach projektu domknięte osobnym commitem przed pierwszym pushem na GitHuba.
- 2026-09-20 | feat(update): ship from GitHub and update in place | Dystrybucja przez GitHub Releases (tag `v*` uruchamia build na Windowsie i wiesza `netdoctor.exe` + `SHA256SUMS`) oraz samoaktualizacja w aplikacji: sprawdzenie raz na start, banner, pobranie i podmiana binarki przez przemianowanie działającego pliku.
- 2026-09-20 | fix(ci): make the release build reproducible | Nowszy rustc na CI odrzucał 13 literałów `1.0` w `Stroke::new`/`width` (parametr to `impl Into<f32>`, fallback przestaje być akceptowany) — literały otagowane `_f32`, a toolchain w workflow przypięty do 1.95.0, żeby wydanie nie padało przez ruch w kanale `stable`.
- 2026-09-20 | fix(autostart): create the Run key when the profile has none | `enable()` otwierał `HKCU\...\Run` zamiast go utworzyć, więc na profilu bez żadnego wpisu startowego autostart zwracał „not found"; błąd wyszedł dopiero na runnerze GitHuba, który jest właśnie takim świeżym profilem.
- 2026-09-20 | fix(live): count uninterrupted time from what was actually watched | Kafelek „Bez przerw" pokazywał „24 h+" po minucie działania świeżej instalacji, bo brak zapisanych awarii brał za czystą dobę; monitor śledzi teraz bieżący nieprzerwany odcinek obserwacji (luka w próbkach > 60 s zaczyna go od nowa), więc licznik mówi tyle, ile naprawdę zmierzono.
- 2026-09-20 | fix(eventlog): call wevtutil by the name that exists | `read_channel` wołało `run("wevtutil.exe")`, a `system_tool` dokleja rozszerzenie, więc ścieżka wychodziła `wevtutil.exe.exe` i cała integracja z dziennikiem Windows zwracała pustkę bez śladu w logach; nazwa poprawiona, `system_tool` obcina teraz istniejący sufiks `.exe`, a dwa nowe testy pilnują nawrotu: skaner źródeł sprawdzający, że każda nazwa podana do `run()` wskazuje na istniejący plik w System32, oraz test odpalający prawdziwe `wevtutil` z kwerendą budowaną przez produkcyjne `channel_args`.
- 2026-09-20 | perf(history): split the outage row from its context | `EVENT_COLUMNS` ciągnęło `context` i `context_end` (33 KB na awarię) w każdej kwerendzie listy, a zakładka Historia woła je co klatkę — 200 awarii kosztowało 8 ms w debug (21,6 ms w release wg audytu) i blokowało zapisy monitora, bo store ma jedno `Mutex<Connection>`. `Event` stracił oba pola, ciężka połowa to osobny `EventContext` czytany przez `event_context(id)` tylko dla rozwiniętego wiersza; `cause::analyse` dostaje `Option<&Evidence>` zamiast parsować samo, `parse_lead` usunięty (wykres czyta ten sam `Evidence`), a wynik parsowania siedzi w `App::outage_detail` do zmiany zaznaczenia. Po zmianie 848 µs. Test `listing_events_does_not_carry_their_context` pilnuje progu.
- 2026-09-20 | perf(opt): read tweak states off the UI thread | `refresh_tweaks` czytało 21 tweaków synchronicznie, a cztery z nich odpalają `netsh`/`powercfg` — zmierzone 369 ms, i to przed pierwszą klatką (`App::new` = 406 ms), przy każdym kliknięciu zakładki Optymalizacja i po każdym apply. Odczyt poszedł na wątek roboczy przez istniejący kanał `Job` (`Job::TweakStates` z numerem generacji, żeby odczyt wyprzedzony przez apply trafił do kosza, a nie na ekran); `App::new` = 658 µs. Zakładka pokazuje „odczytuję" i wyłącza Odśwież oraz Zastosuj wszystko na czas odczytu, a `apply_all_safe` bierze stan z ostatniego odczytu zamiast czytać wszystko drugi raz. Benchmark `read_cost_per_tweak` został w drzewie jako `#[ignore]`.
- 2026-09-20 | fix(settings): make the outage notification switch do something | `notify_on_outage` był zapisywany do JSON i rysowany jako checkbox, ale nie czytał go nikt — jedyny konsument tej różnicy istniał w starej wersji (`legacy-python/netdoc/gui.py:731`). Decyzja logiki wyszła z `drain_snapshots` do wolnej funkcji `ui::announcement`, testowalnej bez kontekstu egui; przełącznik wycisza oba toasty (awaria i powrót), ale nie dotyka `last_status` ani `outage_started`, bo na nich stoi Historia i kafelek „Bez przerw". Trzy testy plus mutacja kontrolna na usuniętej bramce.
- 2026-09-20 | fix(live): tell a lost probe from a break in the recording by asking, not by measuring | Ścieżka nieredukowana wykresu odróżniała zgubioną sondę od przerwy w nagrywaniu po odstępie między sąsiadami, ze stałą 4.0 s zakładającą zamiatanie co sekundę; przy interwale 2 s i wyżej znikały wszystkie znaczniki awarii (odtworzone testem: „probe_interval = 2 s produced 0 marker(s)"). `mark_recording_gaps` zwraca teraz listę przerw, które sam wstawił, a `reduce` porównuje `None` z tą listą — żadnej arytmetyki i żadnego progu. To załatwia też przypadek, którego próg wyprowadzony z interwału nie umie: pierwszą zgubioną sondę zaraz po przerwie, gdzie lewym sąsiadem jest wstawiony znacznik.
- 2026-09-20 | fix(optimize): stop reporting an unreadable registry value as one worth changing | `DwordTweak::read` sklejało `Ok(None)` z `Err(_)` w „nie ustawione, warte zmiany", więc nieczytelna wartość pokazywała Zastosuj, kwalifikowała się do `apply_all_safe` i zapisywała `{"value": null}` jako stan pierwotny — Revert kasował wtedy wartość, która istniała. Naprawa musiała zejść do `winreg`: `read_dword` woła teraz `open_for_read`, które oddaje `Ok(None)` dla nieistniejącego klucza (inaczej popsułyby się wszystkie tweaki z `create: true`, bo ich klucz polityki z definicji nie istnieje) i `Err` tylko przy kluczu, którego nie da się otworzyć. Mapowanie wyszło do `stack::dword_state`, a reguła bulk-apply do `opt::is_safe_candidate`; pięć testów, w tym odczyt `REG_SZ` jako DWORD, który daje błąd bez potrzeby admina.
- 2026-09-21 | fix(optimize): file a tweak's "before" value under the card it was read from | Snapshoty kluczowały się po samym `tweak.id()`, a tweaki związane z kartą rozwiązują klucz rejestru z bieżącego `NetState` — więc apply na karcie A, przełączenie na B i apply ponownie kończyło się tym, że `record_first` widział istniejące id i nie zapisywał nic: B zostawała zmieniona na zawsze, a Revert przywracał A. Trait `Tweak` dostał `scope_key(net)` (domyślnie `id()`), klucz per karta to `id@guid`, deklarują go `AdapterPowerSaving`, `NagleOff` i `AdvTweak`. `snapshot_key_for` sprawdza najpierw klucz z zasięgiem, potem goły `id`, żeby wpisy zapisane przez wcześniejsze buildy nie straciły przycisku Cofnij; cofają się poprawnie, bo każdy taki snapshot trzyma ścieżkę klucza rejestru w sobie. Pięć testów plus mutacja kontrolna.
- 2026-09-21 | fix(netstate): describe the adapter during an outage, and name a failed DHCP lease | `read_adapters` przypisywało wynik tylko przy `gateway.is_some() && up`, więc przy zerwanym łączu zwracało pustkę, monitor odrzucał taki odczyt i UI pokazywał SSID, sygnał i kanał sprzed awarii przez cały czas jej trwania. Odtworzone na żywo (`netsh wlan disconnect` przy `watch_netstate`): przed naprawą `adapter=<none>`, po naprawie `adapter=WiFi up=false ssid=-`. Wybór karty idzie teraz po `GetBestInterface(1.1.1.1)`, a metryka interfejsu rozstrzyga remisy (sprawdzone: `Some(22)` = jedyna trasa domyślna tej maszyny); loopback wykluczony, bo po dopuszczeniu kart bez bramy wygrywałby metryką. Dołożone rozpoznanie APIPA (`diagnose::is_apipa`): 169.254/16 nie jest objęte `Ipv4Addr::is_private`, więc nieudany DHCP był nie do odróżnienia od braku karty; ma teraz własny `Finding` z radą, a `find_edge` i `path::classify` nie biorą już adresu link-local za publiczny. Pomiar pokazał też, że brama i adres lokalny znikają dopiero kilka sekund po utracie asocjacji — detekcji zerwania nie wolno stawiać na `gateway.is_none()`.
- 2026-09-21 | perf(monitor): ping every target at once instead of one after another | Cztery cele pingowane sekwencyjnie na jednym uchwycie kosztowały jeden timeout każdy, więc w czasie awarii kadencja cicho spadała z sekundy do ~4 s — i `mark_recording_gaps` brał te wolne zamiatania za „aplikacja nie nagrywała", czyli mechanizm odróżniający jedno od drugiego działał odwrotnie dokładnie w scenariuszu, dla którego powstał. `PingerPool` daje jeden uchwyt ICMP na cel, każdy wątek bierze swój `Pinger` przez `&mut` (uchwyt jest przenoszony, nie dzielony — `Pinger` jest `Send`, ale nie `Sync`, i tak zostaje). Pomiar: 3,59–3,99 s → 0,50 s przy ustawieniach domyślnych. Sama równoległość nie wystarczyła do kryterium, bo timeout i interwał to domyślnie po sekundzie; stąd `Settings::sweep_timeout_ms()` z sufitem 80% interwału, dotyczący tylko kadencji monitora. Pomiar pokazał też podłogę ~500 ms na ping do nieosiągalnego adresu — skracanie timeoutu poniżej pół sekundy nie daje nic.
- 2026-09-21 | perf(store): give reads their own connection so writes stop queueing behind them | Komentarz przy `Store::open` obiecywał, że WAL nie pozwala pisarzowi blokować odczytów UI — prawda o SQLite, nieprawda o tym kodzie: jedno `Mutex<Connection>` ustawiało obie strony w kolejce w Ruście, zanim SQLite je zobaczył. Zapisy monitora czekały więc na kwerendy wykresu. `Store` ma teraz osobne połączenie do odczytu z `PRAGMA query_only=ON` (dla bazy w pamięci `None`, bo drugie połączenie do `:memory:` to druga, pusta baza). Zmierzone: najgorszy zapis przy czytelniku w pętli spadł z 24–39 ms do 0,36–0,91 ms; test z progiem 8 ms plus mutacja kontrolna.
- 2026-09-21 | feat(update): verify the download against the SHA256SUMS the release publishes | Workflow wieszał plik sum obok binarki od pierwszego wydania, a updater sprawdzał tylko nagłówek `MZ`, minimum 1 MiB i rozmiar z API. `Release` niesie teraz `sums_url`, a `verify` porównuje SHA-256 policzony ze strumienia z wpisem dla `netdoctor.exe`. Hash przez `sha2`, nie przez BCrypt z crate'a `windows`: pięćdziesiąt linii unsafe FFI za skrót to zły kurs w repo, które chce mieć unsafe pod ostrzejszą kontrolą. Wydanie bez pliku sum instaluje się dalej na kontrolach kształtu, bo odmowa zablokowałaby aktualizację ze starego builda. Model zaufania bez zmian — binarka i suma z tego samego miejsca — zamknięte są przypadki losowe, nie przejęte konto. Cztery testy, w tym plik 200 KB sprawdzony przeciwko `Get-FileHash`. Przy okazji poprawione dwa miejsca w dokumentacji, które twierdziły, że updater tego pliku nie czyta.
- 2026-09-21 | feat(single): one instance at a time, and the second raises the first | Dwa procesy pisały do jednej `history.db` po próbce na sekundę na cel; test `a_second_writer_flatters_the_jitter_it_is_measuring` pokazuje, że to nie tylko duplikat historii — przeplot rozsuwa kolejne wiersze, a `Store::stats` czyta odległość między kolejnymi jako jitter, więc druga instancja **zaniża** zmierzoną nierówność łącza. Strażnik to nazwany mutex w przestrzeni `Local\` (nie `Global\`: baza jest per użytkownik, a `Global\` wymaga uprawnienia, którego zwykłe konto nie ma). Druga instancja nie startuje monitora, tylko wynosi okno pierwszej na wierzch — szukane po ścieżce pliku wykonywalnego, nie po tytule, i pokazywane przez `SW_RESTORE` także gdy pierwsza poszła z autostartu z `--minimised`. Zweryfikowane na żywo: drugi proces kończy się kodem 0, zostaje jeden `netdoctor`, a oknem pierwszego planu jest okno pierwszego procesu.
- 2026-09-21 | chore(repo): check the README against the tree, and run the checks on every push | Higiena repo z pozycji 13 planu, bez ruszania historii gita. `__pycache__/run.cpython-313.pyc` odśledzony i wpisany do `.gitignore`, katalog `NetDoctor-1.1.0-windows-x64/` (exe 5,8 MB) usunięty z drzewa roboczego — obiekty zostają w historii, to osobna decyzja. `legacy-python/` zostaje świadomie, bo pozycja 4 tej samej sesji użyła `gui.py:731` jako dowodu pierwotnej intencji; jego README zaczyna się teraz od ramki „zamrożona referencja, nie kod do uruchamiania". README mówił „43 tests" przy 218 w drzewie, więc liczba dostała strażnika: `the_readme_still_quotes_the_right_number_of_tests` skanuje `src/` po atrybutach `#[test]` zamiast pytać harnessa, który nie widzi `--ignored`, a igłę składa przez `concat!`, żeby nie policzyć samego siebie (mutacja kontrolna w obie strony: przestawiona liczba w README i dopisany test w innym pliku). Do README doszło też, że `history.db` stoi na 367 MB przy ustawieniach domyślnych i ile danych kosztuje test obciążenia — plan mówił „bez górnego limitu", pomiar mówi co innego: limit jest czasowy (4 strumienie, 13 s), więc szybsze łącze płaci więcej, około 130 MB na 100 Mbit/s i 1,3 GB na gigabicie. Nowy `ci.yml` puszcza `fmt --check`, clippy i pełne `cargo test` na push do main i na PR, z tym samym pinem toolchaina co release; pełne, bo bieg 35522011862 pokazał, że testy czytające żywą maszynę przechodzą na `windows-latest`. Na koniec `#![deny(unsafe_op_in_unsafe_fn)]` i 14 wynikających poprawek w pięciu `unsafe fn`, przy okazji cztery brakujące sekcje `# Safety`.
- 2026-09-21 | docs: say what this project is, in the three places that ask | Trzy dokumenty jechały z niewypełnionym promptem z szablonu, w tym pierwszy akapit `CLAUDE.md`, czyli kotwica dla każdej decyzji agenta. Do jednego zdania „co to jest i dla kogo" dołożona konsekwencja, bo samo zdanie niczego nie rozstrzyga: werdykt aplikacji jest jej jedynym produktem, więc uczciwe „nie udało się odczytać" bije pewną siebie zgadywankę — pozycje 6 i 8 tego audytu wzięły się dokładnie z raportowania porażki odczytu jako pomiaru. `docs/architecture.md` dostał mapę modułów w czterech grupach plus zasadę o rytmie pomiaru zamiast rytmu klatki. Placeholder ma teraz strażnika (`no_document_still_carries_an_unfilled_placeholder`) dopasowującego imperatyw z szablonu w komentarzu HTML, a nie pustkę: sprawdzanie pustki wymagałoby listy sekcji, którym wolno być pustymi, i gniłoby tak samo. Mutacja kontrolna znalazła w nim dziurę — „Uzupelnij" bez ogonków przechodziło na zielono — więc dopasowanie obejmuje obie formy.
- 2026-09-21 | chore(release): 1.2.0 | Podbicie wersji pod wydanie zamykające czternastopunktowy audyt (commity 4883dcc..d4b07d4). Minor, nie patch: doszły funkcje — weryfikacja pobrania przez SHA-256, strażnik pojedynczej instancji z wyniesieniem okna, rozpoznanie APIPA z własną radą, równoległe zamiatanie z sufitem timeoutu — i nic nie zmieniło się w sposób, który zepsułby istniejącą konfigurację. Przed tagiem sprawdzone lokalnie na buildzie release: `--version` mówi 1.2.0, `--scan --quick` przechodzi całą ścieżkę i kończy zerem, cztery testy `#[ignore]` przechodzą (GetBestInterface wskazuje tę samą kartę co trasa domyślna). Tag rusza `release.yml`, który sam sprawdza zgodność tagu z `Cargo.toml` przed publikacją.
- 2026-09-23 | fix: seven findings from the 2026-09-22 review | Przegląd całego repo znalazł siedem błędów, wszystkie naprawione w jednym commicie. (1) Restart po aktualizacji uruchamiał nową kopię, zanim stara zwolniła mutex pojedynczej instancji, więc nowa od razu się poddawała i nie zostawało nic; teraz dostaje `--after-update` i czeka do 60 s (`single::acquire_within`). Sprawdzone na procesach: stara wersja kończy się bez działającej kopii, nowa przejmuje. (2) Test DNS i rozwiązywanie hostów użytkownika wyjęte z zamiatania na własne wątki; zapytanie wiszące ponad 5 s to awaria. (3) Sen i pauza nie doliczają się do awarii: logika w `monitor::Outages`, dziura ponad 60 s zamyka awarię na ostatnim pomiarze, krótsza pauza jest mostkowana z `unwatched_s`. (4) Kernel-Power 506 (wejście w Modern Standby) rozpoznawany jako sen. (5) `optimize::apply` odmawia tweaka odwracalnego, gdy stanu pierwotnego nie dało się odczytać (wcześniej zapisywało `null` i Cofnij przywracało zmyślone wartości). (6) Pauza użytkownika oddzielona od licznika blokad zadań. (7) Wersja release podłącza się do konsoli rodzica przez `AttachConsole`, więc `--scan`, `--version` i `--help` wreszcie coś wypisują; sprawdzone w prawdziwej konsoli `cmd`, stara wersja wypisywała pustkę. Wątek ścieżki odłączony od zamykania (0,2–0,4 s zamiast do 24 × timeout). 231 testów, clippy czysty.
- 2026-09-23 | feat(tray): a notification-area icon and Windows notifications for outages | Aplikacja chodzi w tle z autostartu, a wszystko, co miała do powiedzenia, było w niewidocznym oknie: dymki o awarii rysowane w egui i brak drogi powrotu do okna poza ponownym uruchomieniem exe. `tray.rs`: własny wątek z ukrytym oknem Win32, ikona w kolorze stanu (szara, gdy nikt nie mierzy), tooltip z werdyktem, menu Otwórz/Zakończ, krzyżyk chowa, balony `Shell_NotifyIconW` tylko o awarii, która trafia do historii i jest twarda, oraz o jej końcu (`monitor::Announcer`). Weryfikacja na żywo znalazła cztery błędy, trzy sprzed tej zmiany: `--minimised` nigdy nie ukrywało okna (eframe 0.29 pokazuje je po pierwszej klatce), szukanie okna po „ma tytuł” trafiało w okna IME i sterownika GL (także w `single::raise_existing_window`), ukryte okno eframe nie przyjmowało WM_CLOSE, a po twardej awarii jej własne straty trzymały łącze w stanie „wolno” do 60 s, przez co każda awaria trwała w historii o minutę za długo. Test z prawdziwym odcięciem Wi-Fi: powiadomienie po 4 s, powrót po 6,6 s, zapis 9,4 s przy odcięciu 6,4 s. Do tego usuwanie ikony w hooku paniki i `--after-update` dla „Uruchom jako administrator”. 246 testów, w tym 3 na żywym zasobniku (`#[ignore]`).
- 2026-09-23 | fix: a probe that could not be sent is not an outage | Gdy nie dało się otworzyć żadnego uchwytu ICMP, pula monitora zwracała pustą listę, a `classify` brał to za brak routera i zapisywał „karta rozłączona” z powiadomieniem; w skanie `ping_series` robił z tego 100% strat i werdykt „LAN, jednoznaczne” przy działającym TCP. Teraz oba zwracają `Err`: monitor traktuje taki przebieg jak pauzę (bez próbki, awarii i powiadomienia) i wystawia `Snapshot.blind`, pokazywane na szaro jako „Brak pomiaru”, a skan zwraca `Segment::Unmeasured`. Test odtwarzający, dwie mutacje kontrolne.
- 2026-09-23 | fix(netstate): an unreadable Wi-Fi state is not a dropped association | `wifi_associated` dawało `false` przy nieudanym otwarciu WLAN i odpowiadało za dowolną kartę Wi-Fi. Teraz bierze GUID karty z trasy domyślnej i zwraca `Option<bool>`; tylko `Some(false)` daje „karta rozłączona”. Sprawdzone na żywo: GUID z listy WLAN pasuje do `adapter_guid`.
- 2026-09-23 | fix(tray): a reading nobody refreshed is grey, not the colour it had | Ikona pokazywała kolor ostatniej migawki bez względu na jej wiek, więc zawieszony wątek zamiatania zostawiał ją zieloną. Pomiar starszy niż 5 interwałów (min. 10 s) daje szarą ikonę i tooltip z wiekiem; logika w czystej funkcji `Seen::of`.
- 2026-09-23 | fix(settings): refuse a probe interval that would hide every outage | Interwał ≥ 60 s sprawiał, że każdy przebieg wyglądał jak sen maszyny i zerował licznik porażek, więc żadna awaria nie mogła się otworzyć. `Settings::refusal` odrzuca powyżej 30 s z komunikatem, `interval()` przycina ręcznie edytowany plik.
- 2026-09-23 | docs: sync notes, architecture, index and README after the phase-one fixes | Zmiany z /sync po fazie 1 zacommitowane osobno, zanim zaczęła się faza 2.
- 2026-09-23 | fix(monitor): a local box that answers does not prove the internet works | Pi-hole, NAS albo resolver na CGNAT, który odpowiadał, liczył się jako „internet działa” i ukrywał awarię ISP; teraz liczy się tylko adres publiczny (`diagnose::is_public`), a CGNAT jest siecią dostawcy, spójnie z `find_edge`.
- 2026-09-23 | fix: a verdict that a segment is down needs more than one reading to agree | Skan mierzył tylko 1.1.1.1, a milczący router wygrywał wszystko, więc router ignorujący pingi dawał „LAN, jednoznaczne”, a odfiltrowane 1.1.1.1 „ISP, jednoznaczne”; doszła druga kotwica 8.8.8.8 (ping i TCP), „internet padł” wymaga zgodności wszystkich pomiarów, a monitor liczy straty, jitter i opóźnienie z najlepszego publicznego celu zamiast z samego 1.1.1.1.
- 2026-09-23 | fix(monitor): record an outage as the worst state it reached | Rodzaj awarii zapisywany raz przy otwarciu zostawiał „degraded” dla awarii, która przeszła w „ISP down”, i pomijał ją w `isp_pattern`; `Recorded` trzyma najpoważniejszy stan według `scope_rank`, a `Store::set_event_kind` przemianowuje otwarty wiersz bez ruszania kontekstu.
- 2026-09-23 | fix(dns): ask the resolver, and let one failed answer be one failed answer | `getaddrinfo` odpowiadał z pamięci podręcznej Windows (zmierzone 0,7 ms wobec 29 ms), więc martwy resolver był niewidoczny, a jedna zgubiona odpowiedź dawała 10 s awarii z powiadomieniem; teraz zapytanie UDP idzie wprost do resolverów karty, awaria wymaga dwóch porażek z rzędu, a po porażce powtórka jest po 2 s.
- 2026-09-23 | fix(cause): a strong signal on 2.4 GHz is a suspicion, not a crowded channel | `airtime_24ghz` twierdził „kanał zatłoczony” z pewnością Likely bez żadnego pomiaru obciążenia; teraz Possible, tytuł mówi tylko to, co zmierzono, a rada odsyła do skanu Wi-Fi.
- 2026-09-23 | fix(cause): a driver fault read off a provider's name is likely, not certain | `log_driver_fault` opiera się na dopasowaniu nazwy dostawcy, nie na udokumentowanym id, więc pewność spadła z Certain na Likely.
- 2026-09-23 | fix(eventlog): name a DHCP failure only on the ids that mean one | Manifest dostawcy pokazał, że 1046, 1047, 50066 i 50067 to zdarzenia informacyjne, a były pewną „awarią dzierżawy”; teraz awaria to 1001, 1002, 1003 i 1006 (1003 tylko Likely), 1005 to zduplikowany adres, a DHCPv6, które ta maszyna bez IPv6 loguje rutynowo, jest pomijane.
- 2026-09-23 | feat: fixes from the audit, cable and TCP checks, a plain-language Live tab | Audyt funkcji (`prompts/audyt-wynik-2026-09-23.md`) znalazł m.in.: Cofnij „Szybkiego DNS" wpisywało serwer z DHCP na sztywno (laptop tracił DNS w każdej innej sieci) — snapshot zapisuje teraz źródło z `NameServer`, stare snapshoty porównywane z `DhcpNameServer`; jeden zgubiony ping routera przepisywał awarię dostawcy na „lan" na stałe — eskalacja wymaga tej samej serii co otwarcie awarii. Nowe: próba TCP na 443 co 2 s, gdy żaden publiczny adres nie odpowiada na ping (sieć blokująca ICMP nie jest już awarią dostawcy); liczniki błędów karty (`GetIfEntry2`) wokół sond Diagnozy, tylko dla kabla, sprawdzone na żywo (1,18 mln ramek, 0 błędów). Zakładka Na żywo zaczyna się od odpowiedzi słowami: łańcuch komputer → router → internet z nieznanym ogniwem na szaro, rada i jeden przycisk; trasa zwinięta.
- 2026-09-23 | feat(game): game mode with a ping overlay and a prepared connection | Wykrywanie LoL/CS2/VALORANT po nazwie procesu (ToolHelp) co 3 s, pomiar co 500 ms w grze, nakładka Win32 (osobne okno, przepuszcza kliknięcia, bez fokusu, nic nie wstrzykuje) z pingiem do routera i internetu oraz stroną skoku. Reguła skoku dobrana na tygodniu prawdziwych pomiarów z `history.db`: pierwszy pomysł (+30 ms na jednym pomiarze) dawał 27 ostrzeżeń/h przy łączu stabilnym w 97%, wybrane +50 ms przez 2 z 3 pomiarów daje ~3,5/h (test `blame_on_recorded_history`). „Przygotuj łącze" (admin) zatrzymuje wuauserv/BITS/DoSvc i wyłącza oszczędzanie energii Wi-Fi, zapisuje w `game_session.json` tylko to, co zmieniło, i cofa po 90 s bez gry i jej klienta, z menu albo przy starcie po awarii; usługi wznowione przez Windows są zatrzymywane ponownie. Nakładka sprawdzona symulacją (`ping.exe` jako `cs2.exe`): widoczna w ≤ 3 s, schowana 3,1 s po końcu. Ścieżka administratora nieprzećwiczona na żywo.
- 2026-09-23 | chore(release): 1.3.0 | Minor: nowe funkcje (tryb gry, próba TCP, liczniki kabla, nowy ekran główny), nic nie psuje istniejącej konfiguracji. Przed tagiem: `cargo fmt --check`, clippy i 295 testów zielone, build release z `--version` 1.3.0 i `--scan --quick` kończącym się kodem 0.
- 2026-09-23 | feat: close the function audit and read the router's own account over UPnP | Punkty audytu #4-#9 i #11-#13: nieudana zmiana nie jest przyczyną awarii, błąd zapisu próbek daje szary `Seen::Unrecorded` zamiast zielonego, jeden próg jittera (Live przestał podwajać `jitter_ok_ms`), Pi-hole/AdGuard bez rady „zmień DNS”, pomiar skutku zmiany (`effect.rs`, doba przed i po), test obciążenia w obie strony z zastrzeżeniem o nasyceniu (chunked POST na `__up` sprawdzony, HTTP 200), trasa nie obwinia ostatniego widocznego hopa, kanały 5/6 GHz blokami 80 MHz; nowy `probe/igd.rs` co 30 s zapisuje stan WAN, licznik i odcisk adresu publicznego routera do tabeli `router`, a cztery reguły przyczyn czytają je wokół awarii (odczyt sprawdzony na żywo, reguły na danych syntetycznych). 327 testów.
- 2026-09-24 | feat(diagnose): the chain and every reading on screen, and an optional AI second opinion | Zakładka Diagnoza pokazuje pod werdyktem rysunek ogniw (komputer → router → dostawca → internet) z milisekundami i stratami każdego ogniwa oraz tabelę wszystkich pomiarów; niezmierzone ogniwo jest przerywane i podpisane, nie zielone. Opcjonalny „drugi głos” przez OpenRouter (domyślnie `deepseek/deepseek-v4-flash`) z kluczem użytkownika, po kliknięciu, z maskowaniem SSID/BSSID/publicznych IP (sprawdzone na prawdziwym skanie) i podglądem wysyłanej treści. Ścieżka sieciowa sprawdzona odrzuconym kluczem (HTTP 401), układ obejrzany w działającej aplikacji; odpowiedź modelu nie oglądana. 338 testów.
- 2026-09-24 | feat(diagnose): a long measurement that catches the drops a scan misses | Opcja 2 lub 5 min w Diagnozie: ping co sekundę do routera, węzła dostawcy i dwóch kotwic na wspólnym zegarze, każda zła sekunda przypisana do ogniwa, na którym zaczyna się kłopot; zryw to ≥2 s straty albo skok >50 ms i ×2, pojedyncza sekunda nie oskarża. Zrywy wchodzą do werdyktu przed historią awarii, spokojny pomiar jej nie kasuje; pasek czasu i tabela zrywów z godziną do sekundy, przerwanie zachowuje nagrane. Nagrywanie sprawdzone na żywo, 2-min pomiar w aplikacji zakończony; karta z wynikiem nieobejrzana. 354 testy.
- 2026-09-24 | feat(diagnose): IPv6, DNS against a public resolver, and the Windows log during the scan | Trzy nowe kontrole: IPv6 rozróżnia brak (Info) od trasy, która nie przenosi ruchu (Warn, naprawa `prefer_ipv4`, bo programy czekają na timeout przed IPv4); DNS przydzielony kontra 1.1.1.1, najlepszy z trzech, ostrzeżenie przy ×2 i +30 ms, Pi-hole bez propozycji wymiany; dziennik Windows od minuty przed skanem do końca, a do werdyktu wchodzą tylko zdarzenia WLAN na Wi-Fi. `--scan --quick` na żywo: IPv6 brak, porównanie DNS pominięte (serwery już publiczne), dziennik czysty. 360 testów.
- 2026-09-24 | feat(overlay): a held lag stays a lag, a card the player sets up, and games only | Przegląd nakładki: odniesienie skoku to 20. percentyl z 120 s (mediana z 10 s po ~5 s lagu mówiła „Stabilnie”; na history.db 6,4 ostrzeżenia/h, 0% przeoczonych lagów), skok za routerem przy ruchu z PC podaje oba fakty; karta z paskiem stanu, LAN/NET, ocena tylko przy problemie; ustawienia róg/treść/rozmiar/widoczność działające od kliknięcia; tylko w grach, Hearthstone dodany. Ping do serwera LoL niemierzalny (Riot Direct milczy na ICMP), odrzucony.
- 2026-09-24 | chore(release): 1.4.0 | Minor: nowe funkcje (Diagnoza z rysunkiem ogniw, długi pomiar, IPv6/DNS/dziennik w skanie, opcjonalne AI, nakładka w nowej formie z ustawieniami, Hearthstone), bez zmian wymagających konfiguracji. Przed tagiem: `cargo fmt --check`, clippy i 353 testy zielone, build release z `--version` 1.4.0 i `--scan --quick` przechodzącym całą ścieżkę (kod 1 z awarii zapisanych w ostatnich 24 h, zgodnie z dokumentacją).
- 2026-09-24 | feat(settings): pages, a pinned save bar, and changes you can see before saving | Zakładka Ustawienia podzielona na strony (Pomiar, Cele pingowania, Nakładka w grze, Ogólne, Wyjaśnienie AI, Aktualizacje) z listą po lewej i rzędem u góry w wąskim oknie; pasek zapisu przypięty na dole ze stanem „Niezapisane zmiany”, Zapisz aktywne tylko przy zmianach, Odrzuć zmiany, Ctrl+S; żółta kropka przy stronie z niezapisaną zmianą i „•” przy zakładce, niebieska przy czekającej aktualizacji; jednostki przy polach zamiast w etykietach, zakresy pól; „Przywróć domyślne” już nie zapisuje od razu i zachowuje język oraz klucz AI. Clippy i 353 testy zielone, aplikacja uruchomiona i obejrzana przez użytkownika.

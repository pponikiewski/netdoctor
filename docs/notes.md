# netdoc — Session Notes
<!-- One line per commit, appended via /sync: date | commit subject | what/why -->

- 2026-09-20 | feat: enhance outage analysis and logging with new metrics and improved handling | Rozbudowa analizy awarii i zapisu zdarzeń w cause/diagnose/store, żeby historia niosła więcej niż sam fakt przerwy.
- 2026-09-20 | feat(live): make the chart and the cards readable without prior knowledge | Zakładka Na żywo tłumaczy teraz każdą liczbę: klikalne chipy legendy z odczytem, wyjaśnienia celów i kafelków pod kursorem, podpisane progi, uporządkowana legenda pod wykresem; przy okazji usunięte zapytania DNS i SQLite wykonywane przy każdej klatce.
- 2026-09-20 | style: format the codebase with rustfmt, pinned by rustfmt.toml | Kod był formatowany ręcznie i nie pasował do żadnej konfiguracji rustfmt, więc `cargo fmt` wymagany przez CLAUDE.md rozsypywał diff po kilkunastu modułach; jednorazowe przeformatowanie z przypiętym `use_small_heuristics = "Max"`.
- 2026-09-20 | chore: keep the formatting commit out of git blame | `.git-blame-ignore-revs` z hashem commita formatującego, żeby blame dalej wskazywał autora treści, a nie przebiegu formatowania.
- 2026-09-20 | docs: sync notes, architecture and project instructions | Zaległe zmiany w dokumentach i instrukcjach projektu domknięte osobnym commitem przed pierwszym pushem na GitHuba.
- 2026-09-20 | feat(update): ship from GitHub and update in place | Dystrybucja przez GitHub Releases (tag `v*` uruchamia build na Windowsie i wiesza `netdoctor.exe` + `SHA256SUMS`) oraz samoaktualizacja w aplikacji: sprawdzenie raz na start, banner, pobranie i podmiana binarki przez przemianowanie działającego pliku.
- 2026-09-20 | fix(ci): make the release build reproducible | Nowszy rustc na CI odrzucał 13 literałów `1.0` w `Stroke::new`/`width` (parametr to `impl Into<f32>`, fallback przestaje być akceptowany) — literały otagowane `_f32`, a toolchain w workflow przypięty do 1.95.0, żeby wydanie nie padało przez ruch w kanale `stable`.
- 2026-09-20 | fix(autostart): create the Run key when the profile has none | `enable()` otwierał `HKCU\...\Run` zamiast go utworzyć, więc na profilu bez żadnego wpisu startowego autostart zwracał „not found"; błąd wyszedł dopiero na runnerze GitHuba, który jest właśnie takim świeżym profilem.
- 2026-09-20 | fix(live): count uninterrupted time from what was actually watched | Kafelek „Bez przerw" pokazywał „24 h+" po minucie działania świeżej instalacji, bo brak zapisanych awarii brał za czystą dobę; monitor śledzi teraz bieżący nieprzerwany odcinek obserwacji (luka w próbkach > 60 s zaczyna go od nowa), więc licznik mówi tyle, ile naprawdę zmierzono.
- 2026-09-20 | fix(eventlog): call wevtutil by the name that exists | `read_channel` wołało `run("wevtutil.exe")`, a `system_tool` dokleja rozszerzenie, więc ścieżka wychodziła `wevtutil.exe.exe` i cała integracja z dziennikiem Windows zwracała pustkę bez śladu w logach; nazwa poprawiona, `system_tool` obcina teraz istniejący sufiks `.exe`, a dwa nowe testy pilnują nawrotu: skaner źródeł sprawdzający, że każda nazwa podana do `run()` wskazuje na istniejący plik w System32, oraz test odpalający prawdziwe `wevtutil` z kwerendą budowaną przez produkcyjne `channel_args`.
- 2026-09-20 | perf(history): split the outage row from its context | `EVENT_COLUMNS` ciągnęło `context` i `context_end` (33 KB na awarię) w każdej kwerendzie listy, a zakładka Historia woła je co klatkę — 200 awarii kosztowało 8 ms w debug (21,6 ms w release wg audytu) i blokowało zapisy monitora, bo store ma jedno `Mutex<Connection>`. `Event` stracił oba pola, ciężka połowa to osobny `EventContext` czytany przez `event_context(id)` tylko dla rozwiniętego wiersza; `cause::analyse` dostaje `Option<&Evidence>` zamiast parsować samo, `parse_lead` usunięty (wykres czyta ten sam `Evidence`), a wynik parsowania siedzi w `App::outage_detail` do zmiany zaznaczenia. Po zmianie 848 µs. Test `listing_events_does_not_carry_their_context` pilnuje progu.
- 2026-09-20 | perf(opt): read tweak states off the UI thread | `refresh_tweaks` czytało 21 tweaków synchronicznie, a cztery z nich odpalają `netsh`/`powercfg` — zmierzone 369 ms, i to przed pierwszą klatką (`App::new` = 406 ms), przy każdym kliknięciu zakładki Optymalizacja i po każdym apply. Odczyt poszedł na wątek roboczy przez istniejący kanał `Job` (`Job::TweakStates` z numerem generacji, żeby odczyt wyprzedzony przez apply trafił do kosza, a nie na ekran); `App::new` = 658 µs. Zakładka pokazuje „odczytuję" i wyłącza Odśwież oraz Zastosuj wszystko na czas odczytu, a `apply_all_safe` bierze stan z ostatniego odczytu zamiast czytać wszystko drugi raz. Benchmark `read_cost_per_tweak` został w drzewie jako `#[ignore]`.
- 2026-09-20 | fix(settings): make the outage notification switch do something | `notify_on_outage` był zapisywany do JSON i rysowany jako checkbox, ale nie czytał go nikt — jedyny konsument tej różnicy istniał w starej wersji (`legacy-python/netdoc/gui.py:731`). Decyzja logiki wyszła z `drain_snapshots` do wolnej funkcji `ui::announcement`, testowalnej bez kontekstu egui; przełącznik wycisza oba toasty (awaria i powrót), ale nie dotyka `last_status` ani `outage_started`, bo na nich stoi Historia i kafelek „Bez przerw". Trzy testy plus mutacja kontrolna na usuniętej bramce.
- 2026-09-20 | fix(live): tell a lost probe from a break in the recording by asking, not by measuring | Ścieżka nieredukowana wykresu odróżniała zgubioną sondę od przerwy w nagrywaniu po odstępie między sąsiadami, ze stałą 4.0 s zakładającą zamiatanie co sekundę; przy interwale 2 s i wyżej znikały wszystkie znaczniki awarii (odtworzone testem: „probe_interval = 2 s produced 0 marker(s)"). `mark_recording_gaps` zwraca teraz listę przerw, które sam wstawił, a `reduce` porównuje `None` z tą listą — żadnej arytmetyki i żadnego progu. To załatwia też przypadek, którego próg wyprowadzony z interwału nie umie: pierwszą zgubioną sondę zaraz po przerwie, gdzie lewym sąsiadem jest wstawiony znacznik.
- 2026-09-20 | fix(optimize): stop reporting an unreadable registry value as one worth changing | `DwordTweak::read` sklejało `Ok(None)` z `Err(_)` w „nie ustawione, warte zmiany", więc nieczytelna wartość pokazywała Zastosuj, kwalifikowała się do `apply_all_safe` i zapisywała `{"value": null}` jako stan pierwotny — Revert kasował wtedy wartość, która istniała. Naprawa musiała zejść do `winreg`: `read_dword` woła teraz `open_for_read`, które oddaje `Ok(None)` dla nieistniejącego klucza (inaczej popsułyby się wszystkie tweaki z `create: true`, bo ich klucz polityki z definicji nie istnieje) i `Err` tylko przy kluczu, którego nie da się otworzyć. Mapowanie wyszło do `stack::dword_state`, a reguła bulk-apply do `opt::is_safe_candidate`; pięć testów, w tym odczyt `REG_SZ` jako DWORD, który daje błąd bez potrzeby admina.
- 2026-09-21 | fix(optimize): file a tweak's "before" value under the card it was read from | Snapshoty kluczowały się po samym `tweak.id()`, a tweaki związane z kartą rozwiązują klucz rejestru z bieżącego `NetState` — więc apply na karcie A, przełączenie na B i apply ponownie kończyło się tym, że `record_first` widział istniejące id i nie zapisywał nic: B zostawała zmieniona na zawsze, a Revert przywracał A. Trait `Tweak` dostał `scope_key(net)` (domyślnie `id()`), klucz per karta to `id@guid`, deklarują go `AdapterPowerSaving`, `NagleOff` i `AdvTweak`. `snapshot_key_for` sprawdza najpierw klucz z zasięgiem, potem goły `id`, żeby wpisy zapisane przez wcześniejsze buildy nie straciły przycisku Cofnij; cofają się poprawnie, bo każdy taki snapshot trzyma ścieżkę klucza rejestru w sobie. Pięć testów plus mutacja kontrolna.
- 2026-09-21 | fix(netstate): describe the adapter during an outage, and name a failed DHCP lease | `read_adapters` przypisywało wynik tylko przy `gateway.is_some() && up`, więc przy zerwanym łączu zwracało pustkę, monitor odrzucał taki odczyt i UI pokazywał SSID, sygnał i kanał sprzed awarii przez cały czas jej trwania. Odtworzone na żywo (`netsh wlan disconnect` przy `watch_netstate`): przed naprawą `adapter=<none>`, po naprawie `adapter=WiFi up=false ssid=-`. Wybór karty idzie teraz po `GetBestInterface(1.1.1.1)`, a metryka interfejsu rozstrzyga remisy (sprawdzone: `Some(22)` = jedyna trasa domyślna tej maszyny); loopback wykluczony, bo po dopuszczeniu kart bez bramy wygrywałby metryką. Dołożone rozpoznanie APIPA (`diagnose::is_apipa`): 169.254/16 nie jest objęte `Ipv4Addr::is_private`, więc nieudany DHCP był nie do odróżnienia od braku karty; ma teraz własny `Finding` z radą, a `find_edge` i `path::classify` nie biorą już adresu link-local za publiczny. Pomiar pokazał też, że brama i adres lokalny znikają dopiero kilka sekund po utracie asocjacji — detekcji zerwania nie wolno stawiać na `gateway.is_none()`.
- 2026-09-21 | perf(monitor): ping every target at once instead of one after another | Cztery cele pingowane sekwencyjnie na jednym uchwycie kosztowały jeden timeout każdy, więc w czasie awarii kadencja cicho spadała z sekundy do ~4 s — i `mark_recording_gaps` brał te wolne zamiatania za „aplikacja nie nagrywała", czyli mechanizm odróżniający jedno od drugiego działał odwrotnie dokładnie w scenariuszu, dla którego powstał. `PingerPool` daje jeden uchwyt ICMP na cel, każdy wątek bierze swój `Pinger` przez `&mut` (uchwyt jest przenoszony, nie dzielony — `Pinger` jest `Send`, ale nie `Sync`, i tak zostaje). Pomiar: 3,59–3,99 s → 0,50 s przy ustawieniach domyślnych. Sama równoległość nie wystarczyła do kryterium, bo timeout i interwał to domyślnie po sekundzie; stąd `Settings::sweep_timeout_ms()` z sufitem 80% interwału, dotyczący tylko kadencji monitora. Pomiar pokazał też podłogę ~500 ms na ping do nieosiągalnego adresu — skracanie timeoutu poniżej pół sekundy nie daje nic.
- 2026-09-21 | perf(store): give reads their own connection so writes stop queueing behind them | Komentarz przy `Store::open` obiecywał, że WAL nie pozwala pisarzowi blokować odczytów UI — prawda o SQLite, nieprawda o tym kodzie: jedno `Mutex<Connection>` ustawiało obie strony w kolejce w Ruście, zanim SQLite je zobaczył. Zapisy monitora czekały więc na kwerendy wykresu. `Store` ma teraz osobne połączenie do odczytu z `PRAGMA query_only=ON` (dla bazy w pamięci `None`, bo drugie połączenie do `:memory:` to druga, pusta baza). Zmierzone: najgorszy zapis przy czytelniku w pętli spadł z 24–39 ms do 0,36–0,91 ms; test z progiem 8 ms plus mutacja kontrolna.
- 2026-09-21 | feat(update): verify the download against the SHA256SUMS the release publishes | Workflow wieszał plik sum obok binarki od pierwszego wydania, a updater sprawdzał tylko nagłówek `MZ`, minimum 1 MiB i rozmiar z API. `Release` niesie teraz `sums_url`, a `verify` porównuje SHA-256 policzony ze strumienia z wpisem dla `netdoctor.exe`. Hash przez `sha2`, nie przez BCrypt z crate'a `windows`: pięćdziesiąt linii unsafe FFI za skrót to zły kurs w repo, które chce mieć unsafe pod ostrzejszą kontrolą. Wydanie bez pliku sum instaluje się dalej na kontrolach kształtu, bo odmowa zablokowałaby aktualizację ze starego builda. Model zaufania bez zmian — binarka i suma z tego samego miejsca — zamknięte są przypadki losowe, nie przejęte konto. Cztery testy, w tym plik 200 KB sprawdzony przeciwko `Get-FileHash`. Przy okazji poprawione dwa miejsca w dokumentacji, które twierdziły, że updater tego pliku nie czyta.
- 2026-09-21 | feat(single): one instance at a time, and the second raises the first | Dwa procesy pisały do jednej `history.db` po próbce na sekundę na cel; test `a_second_writer_flatters_the_jitter_it_is_measuring` pokazuje, że to nie tylko duplikat historii — przeplot rozsuwa kolejne wiersze, a `Store::stats` czyta odległość między kolejnymi jako jitter, więc druga instancja **zaniża** zmierzoną nierówność łącza. Strażnik to nazwany mutex w przestrzeni `Local\` (nie `Global\`: baza jest per użytkownik, a `Global\` wymaga uprawnienia, którego zwykłe konto nie ma). Druga instancja nie startuje monitora, tylko wynosi okno pierwszej na wierzch — szukane po ścieżce pliku wykonywalnego, nie po tytule, i pokazywane przez `SW_RESTORE` także gdy pierwsza poszła z autostartu z `--minimised`. Zweryfikowane na żywo: drugi proces kończy się kodem 0, zostaje jeden `netdoctor`, a oknem pierwszego planu jest okno pierwszego procesu.
- 2026-09-21 | chore(repo): check the README against the tree, and run the checks on every push | Higiena repo z pozycji 13 planu, bez ruszania historii gita. `__pycache__/run.cpython-313.pyc` odśledzony i wpisany do `.gitignore`, katalog `NetDoctor-1.1.0-windows-x64/` (exe 5,8 MB) usunięty z drzewa roboczego — obiekty zostają w historii, to osobna decyzja. `legacy-python/` zostaje świadomie, bo pozycja 4 tej samej sesji użyła `gui.py:731` jako dowodu pierwotnej intencji; jego README zaczyna się teraz od ramki „zamrożona referencja, nie kod do uruchamiania". README mówił „43 tests" przy 218 w drzewie, więc liczba dostała strażnika: `the_readme_still_quotes_the_right_number_of_tests` skanuje `src/` po atrybutach `#[test]` zamiast pytać harnessa, który nie widzi `--ignored`, a igłę składa przez `concat!`, żeby nie policzyć samego siebie (mutacja kontrolna w obie strony: przestawiona liczba w README i dopisany test w innym pliku). Do README doszło też, że `history.db` stoi na 367 MB przy ustawieniach domyślnych i ile danych kosztuje test obciążenia — plan mówił „bez górnego limitu", pomiar mówi co innego: limit jest czasowy (4 strumienie, 13 s), więc szybsze łącze płaci więcej, około 130 MB na 100 Mbit/s i 1,3 GB na gigabicie. Nowy `ci.yml` puszcza `fmt --check`, clippy i pełne `cargo test` na push do main i na PR, z tym samym pinem toolchaina co release; pełne, bo bieg 35522011862 pokazał, że testy czytające żywą maszynę przechodzą na `windows-latest`. Na koniec `#![deny(unsafe_op_in_unsafe_fn)]` i 14 wynikających poprawek w pięciu `unsafe fn`, przy okazji cztery brakujące sekcje `# Safety`.
